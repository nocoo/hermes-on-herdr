#!/usr/bin/python3 -IS
"""Bootstrap the managed runtime, or open recovery with system Python."""

from pathlib import Path
import sys

sys.dont_write_bytecode = True
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from hermes_gateway_herdr.bootstrap import main

raise SystemExit(main())
