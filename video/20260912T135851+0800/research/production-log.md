# Preparation record

On September 12, 2026 the user resumed a bounded asset-preparation task while Hexly's coordinator retained sole ownership of the standard video kit. The Hermes repository started clean on `main` at `cfd414b59c34b998b3fb489142a365970b99e0d0`, matching remote main. No repository or ancestor AGENTS/CLAUDE instruction was found. All writes for this task are inside this dated directory.

The primary coordinator authored fixtures, English short copy, the asset manifest and rebuilding/validation scripts. The existing Grok pane performed read-only icon/license and narrative reviews. It did not write files, run the displayed CLI tasks, record messages or start rendering.

The icon review distinguished the upstream Unicode SVG favicon from the docs navbar raster reference. Both original files were fetched from Hermes Agent revision `b7ac3ba1cdf89f94dfe86de27e01358b194f4053`, compared to the reported hashes, and retained with the MIT license. The favicon was not redrawn. Hexly's favicon was fetched from public GitHub main at the recorded immutable revision. Exact existing brand values and component references were read from committed source; no uncommitted kit files were consumed.

The authored CLI fixtures use short invented tasks. The two monitor views reuse `DashboardView` and `demo_snapshot`; the synthetic profile names, homes, PIDs, pane markers and events are replaced before rendering. Generation runs with process/socket/monitor constructors disabled so an accidental live probe fails. The source's actual terminal theme is preserved inside a light asset frame.

The asset storyboard puts the missing session context first. The reviewer asked that online/channel-verification conditions remain visible in a later composition, not only in production notes; those constraints are retained as required visible captions. No final timing or narration was produced.

The kit's committed API documentation was readable at `e1b220a7643e8275134b0bff0a11d703c047abbe`, but no published SHA had been delivered to this preparation. The public schema URL returned HTTP 403. This optional spike stopped at read-only API research; no failed or unverified runtime adapter is presented as working. Shared-template consumption was provisional at that point.

The verification record covers deterministic asset rebuilding, source hashes, native offline UI checks, local links, static desktop/mobile preview and privacy inspection. It also compares both old MP4 hashes and the old archive's Git tree. It does not claim final media, TTS, playback, audiovisual mastering or release verification.

After the coordinator supplied the published SHA, remote main and the peeled `v0.6.0` tag were independently matched to it. The preparation is now a published-reference archive; full template consumption starts in the next production.

Visual inspection caught a source-identity ambiguity: `logo.png` is a monochrome avatar. The original was moved into research, removed from the Hermes face texture, and is no longer described as a product mark. The exact Unicode SVG remains preserved. A second read-only source review was requested. CLI spacing was corrected with SVG whitespace preservation.
