# Asset preparation verification

Scope: the dated preparation directory only. No voice, music, final film, or shared template runtime is produced by this archive.

- Nine authoritative imported files have pinned URLs, byte counts and SHA-256 hashes. The exact Hermes SVG is a Unicode favicon wrapper. The docs navbar avatar is retained under research, excluded from cubie textures.
- Seven authored terminal fixtures and two native offline monitor views export SVG, text and ANSI. The view builder disables live socket, process and Monitor construction; all identities and task text are synthetic.
- Eighteen face textures map to 27 distinct positions in a complete 3 × 3 cube. All 49 generated files are checked against an in-memory deterministic rebuild.
- The existing dashboard-view suite passed all 22 tests. Output: [dashboard-view-tests.txt](dashboard-view-tests.txt).
- Real Chrome captures cover the native terminal sheet (1836 × 1494), cubie sheet (1782 × 1212), desktop gallery (1920 × 1080) and mobile gallery (390 × 844). The preview checks image decoding, page errors, layout overflow and absence of external requests. Evidence: [browser.json](browser.json), [desktop](gallery-desktop.png), [mobile](gallery-mobile.png).
- The programmatic checker validates source hashes, local links, Python syntax, SVG safety, native aspect ratios, fixture privacy, storyboard references and generated hashes. Its current machine-readable result is [checks.json](checks.json).
- The old production's Git tree remains `23e9cf0dc1e3e246b77bafe4d1d9843dbd8cf323`. Both old MP4 SHA-256 hashes match the saved baseline.
- Hexly remote main and peeled tag `v0.6.0` resolve to the coordinator's published `e1b220a7643e8275134b0bff0a11d703c047abbe`. This archive references its public interfaces; the film will consume the complete pinned package separately.

Visual inspection corrected collapsed terminal spacing and removed the ambiguous docs avatar from the Hermes face. Native monitor screens retain their real terminal theme inside the light preparation layout. Channel scenes carry explicit verification limits and host-online conditions.

These checks establish an asset preparation result. They do not establish a fresh live message-to-pane round trip, integration on Telegram/Slack, a final video render, audio mastering or playback.
