# 11 · 研究与验证记录

研究日期：2026-09-11，时区Asia/Shanghai。本文件保存设计提交 `cc89e95` 时的研究记录；后续实现、测试与当前状态见 [12 · 离线实现与验证](12-离线实现与验证.md)。本记录区分实际执行V、源码阅读S、在线文档D与未运行X，不把设计算法写成集成测试成功。

## 11.1 实际版本与只读基线

Herdr版本输出：

```text
herdr 0.9.0
```

本机binary为 `/opt/homebrew/bin/herdr`。reference仓库HEAD与 `v0.9.0^{commit}` 都是 `b99002ac99b09e00b4ca692436cb15a6b0d676f1`。这证明源码tag匹配和本机版本号一致，没有证明binary与该SHA可复现构建一致。

`hermes --version`实际完整输出：

```text
Hermes Agent v0.21.1 (2026.9.7) · upstream b7ac3ba1
Install directory: /Users/nocoo/.hermes/hermes-agent
Install method: git
Python: 3.11.15
OpenAI SDK: 2.24.0
Update available: 514 commits behind — run 'hermes update'
```

Hermes HEAD为 `b7ac3ba1cdf89f94dfe86de27e01358b194f4053`；CLI位置 `/Users/nocoo/.hermes/hermes-agent/venv/bin/hermes`。只读隔离Python probe确认Python3.11.15、psutil7.2.2。没有按更新提示执行update。

八个本地源码仓库状态基线：Herdr及六个reference clean；Hermes tracked clean、原有untracked `uv.lock.pypi-orig` 保留。所有本轮新增和编辑仅在本项目目录。具体remote/SHA表见 [09](09-源码证据索引.md)。

只读命令形状包括：

```sh
git -C /Users/nocoo/workspace/references/herdr rev-parse HEAD
git -C /Users/nocoo/workspace/references/herdr rev-parse 'v0.9.0^{commit}'
git -C /Users/nocoo/.hermes/hermes-agent rev-parse HEAD
herdr --version
hermes --version
```

源码定位使用rg与按行读取；没有git checkout/reset/pull、没有修改reference或Hermes源码。

## 11.2 官方文档抓取

抓取记录起点：`2026-09-11T04:43:27.427604+00:00`。以下是实际响应body的SHA256，供此次研究复核；页面日后变化不自动更新本设计。HTML整页和临时下载不提交到公开仓库。

| 文档 | HTTP | bytes | 响应SHA256 |
|---|---:|---:|---|
| [llms.txt](https://hermes-agent.nousresearch.com/docs/llms.txt) | 200 | 39921 | `26dafe98c0f4a5ba134b352de5984297f2ab811fdb74c339975e3ea4c9d940b8` |
| [Profiles](https://hermes-agent.nousresearch.com/docs/user-guide/profiles) | 200 | 79016 | `9e9429efaa640b84cebb4004152a3b491cac4ed4ce4c02bffd3e32f0665e1e6d` |
| [Gateway internals](https://hermes-agent.nousresearch.com/docs/developer-guide/gateway-internals) | 200 | 59260 | `c18ccd79e28bc688dbd618623d3a748d3bc76a0e89a10bff5b77f166ce732fe9` |
| [Plugins user guide](https://hermes-agent.nousresearch.com/docs/user-guide/features/plugins) | 200 | 161611 | `3088b527039e807f39ad357a99c86f6d95f8f5b4eeec666c43e6c30c130f944e` |
| [旧build-a-hermes-plugin入口](https://hermes-agent.nousresearch.com/docs/guides/build-a-hermes-plugin) | 200 | 352 | `820e0bafb74edef51346aa1b112cb3056cc3738b1ddd781323ebb9643a0d25f1` |
| [实际developer plugins正文](https://hermes-agent.nousresearch.com/docs/developer-guide/plugins) | 200 | 488512 | `bdfceb5e6fc906cb1ef163bfcd5e7e1902db95485ed93f0528e3719e95242631` |

旧入口的352字节只是HTML meta refresh；已经跟进并阅读真正正文，不能把HTTP200等同于拿到文档内容。Profiles最新版的clone-all/cron描述与固定源码不一致，详情已记入 [M02](09-源码证据索引.md#m02)。

## 11.3 Prior-art 搜索与本机 integration

GitHub查询起点 `2026-09-11T04:43:46.072097+00:00`，通过个人gh配置执行8次repos/code搜索，全部exit0。原样查询、返回数和候选pinned SHA见 [09搜索记录](09-源码证据索引.md#search)。关键发现是auto-reconcile、名为herdr-gateway的Hermes插件和通用restart-always；没有找到所审范围内满足全部目标的exact implementation。

已有本机 `/Users/nocoo/.hermes/plugins/herdr-agent-state` 与Herdr pinned integration assets一致：

| 文件 | SHA256 |
|---|---|
| plugin.yaml | `e1c775a6d6ccad2f8e6e0566a0aa61aeef9b8141f59d8a742a77c6951a261170` |
| __init__.py | `62884f3e0f714f78bfc42722cddef3dd32a0a184502864d79d113329563deed2` |

它只处理cli/tui/desktop/acp会话恢复，不覆盖Gateway生命周期。[H15](09-源码证据索引.md#h15) 本轮只读取这两个integration文件，没有修改用户Profile或插件配置，也没有读取公开报告不需要的secret内容。

## 11.4 文档交付检查

以下检查已实际执行并通过：

| 检查 | 结果与边界 |
|---|---|
| 必需文件和索引 | 13个Markdown文件齐全；根README及docs索引覆盖01–11 |
| 中文正文 | 每个Markdown含至少200个中文字符；人工审阅中文说明、证据与提案的区分 |
| Markdown渲染／链接 | marked17.0.6解析并生成13份HTML字符串；163处相对链接／锚点全部有效 |
| Mermaid | mermaid11.17.2在jsdom26.1.0环境解析5个图，零语法错误；未做浏览器像素／排版截图验证 |
| 示例与围栏 | 围栏闭合；3个JSON、1个JSONL、1个TOML示例解析成功；15个sh代码块通过zsh语法检查；INI为设计模板 |
| Hermes命令 | 从固定源码AST提取纯argparse builders，21条不同命令示例解析成功；未导入main、未调用handler、未执行Profile/Gateway动作；-p按预解析规则处理 |
| 源码定位 | 94处本地pinned源码链接的SHA、文件路径和行号有效；symbol语义来自研究阅读，自动检查不替代语义复核 |
| Secret | 私钥／常见GitHub、provider、AWS、bot token及URL内凭据模式扫描零命中；人工diff未发现硬编码secret，模式扫描不等于完整泄露证明 |
| 暂存区与diff | 显式stage的14个文件仅为13份Markdown与.gitignore；git diff --cached --check通过，无runtime、下载或验证依赖入库 |
| 只读仓库复核 | 八个源码仓库HEAD与状态和基线一致；Hermes原有uv.lock.pypi-orig保留 |

验证使用Node26.8.1、npm11.19.0，以及Hermes venv的Python3.11.15；只执行文档解析和纯parser函数。没有把这些检查算作产品单元／集成／E2E测试。

检查工具和下载缓存只放在项目内 `.research/`，该目录已gitignore，不属于发布文件。不会为了文档验证安装全局npm/Python包，或向reference目录写测试输出。

## 11.5 研究阶段明确没有执行

| 项目 | 状态 |
|---|---|
| runtime实现、安装manifest或插件 | NOT RUN／本阶段非目标 |
| 创建／克隆／修改任何用户Hermes Profile | NOT RUN |
| 配置模型／bot token／OAuth，复制secret | NOT RUN |
| 启动第二个Gateway或隔离测试Gateway | NOT RUN |
| Herdr cold restore/live handoff/crash故障注入 | NOT RUN；只有固定源码证据 |
| Gateway75/0/2/78的真实进程试验 | NOT RUN；有源码分支，CLI坏配置另有未决项 |
| 独立bot消息往返及Agent控制Herdr | NOT RUN |
| launchd/systemd安装、native handoff、sleep/wake | NOT RUN |
| Profile空白暂存创建／迁入 | NOT RUN；必须先做P0-06 |

以上研究阶段可以报告“设计可行且证据已定位”。后续离线代码和测试见12；仍不能报告“已通过真实E2E”或“已保证Herdr崩溃无孤儿”。剩余关键问题和放行标准见 [05](05-实现步骤.md)、[06](06-测试与验证.md)、[10](10-实施任务清单.md)。
